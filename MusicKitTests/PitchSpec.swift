import Quick
import Nimble

class PitchSpec: QuickSpec {
    override func spec() {
        describe("PitchClass") {
            it("should pass") {
                expect(true).to(beTrue())
            }
        }

    }
}
